/*6. Create an object called person with name = John, age = 50, Then,
   access the object to alert("John is 50").*/

// create object 
const person = {
  name: "John",
  age: 50
};

// access the object to alert
alert(person.name + " is " + person.age);